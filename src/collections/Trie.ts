/*
a trie is where each letter is a key and the node is the word

dictionary = {}

add (word)
  keys = new Stack
  keys.add(...word.to_lower.split)
  let node = { keys.pop() : word }
  while !keys.isEmpty
    n
*/
